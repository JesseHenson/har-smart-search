.venv/
__pycache__/
*.pyc
.pytest_cache/
*.db
dist/
build/
lib/
.superpowers/
*.mcpb
.impeccable/

# The bundled interpreter and vendored wheels are build outputs, fetched by
# BUNDLE.md rather than tracked. Together they are ~100MB of binaries that
# would sit in every clone forever.
runtime/
lib/
*.mcpb
