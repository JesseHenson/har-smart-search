{
  "manifest_version": "0.1",
  "name": "har-smart-search",
  "display_name": "HAR Smart Search",
  "version": "0.8.1",
  "description": "Similarity search and comparable-sales valuation over HAR listings.",
  "author": {
    "name": "Jesse Henson"
  },
  "server": {
    "type": "python",
    "entry_point": "src/har_search/server/__main__.py",
    "mcp_config": {
      "command": "/bin/sh",
      "args": [
        "${__dirname}/bin/launch.sh"
      ],
      "env": {
        "PYTHONPATH": "${__dirname}/src",
        "HAR_APIFY_TOKEN": "${user_config.apify_token}",
        "HAR_DASHBOARD_PORT": "${user_config.dashboard_port}"
      }
    }
  },
  "tools": [
    {
      "name": "getting_started",
      "description": "Start here \u2014 walks through a first search end to end and sets up a weekly schedule."
    },
    {
      "name": "plan_search",
      "description": "Build a complete set of search criteria before running anything."
    },
    {
      "name": "search",
      "description": "Search HAR listings by loose criteria and rank by similarity."
    },
    {
      "name": "explain",
      "description": "Show the similarity breakdown and comparable sales for one listing."
    },
    {
      "name": "whats_new",
      "description": "Compare the two most recent runs of a saved search."
    },
    {
      "name": "open_dashboard",
      "description": "Return the local dashboard URL."
    }
  ],
  "user_config": {
    "apify_token": {
      "type": "string",
      "title": "Apify API token",
      "description": "Used to fetch HAR listing data. Stored locally, never shared.",
      "sensitive": true,
      "required": true
    },
    "dashboard_port": {
      "type": "number",
      "title": "Dashboard port",
      "description": "Local port for the results dashboard. Change it only if something else on this machine already uses 7788.",
      "default": 7788,
      "required": false
    }
  },
  "compatibility": {
    "platforms": [
      "darwin"
    ]
  },
  "prompts": [
    {
      "name": "getting_started",
      "description": "Walk through a first search end to end: gather criteria, run it, explain the dashboard, and offer a weekly schedule.",
      "text": "Help me set up my first HAR Smart Search, one step at a time."
    },
    {
      "name": "plan_search",
      "description": "Assemble a complete, well-formed set of search criteria before running anything.",
      "text": "Help me build a complete HAR search before running it."
    }
  ]
}
